# Compiler
CC = g++

# Compiler flags
CFLAGS = -std=c++17 -Wall

# Target executable name
TARGET = wordCounter

# Source files
SRC = wordCounter.cpp

# Default target
all: $(TARGET)

$(TARGET): $(SRC)
	$(CC) $(CFLAGS) $(SRC) -o $(TARGET)

clean:
	rm -f $(TARGET)
